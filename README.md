# Jarurat-Care-Internship-Assignment
I built a Python automation script using requests and BeautifulSoup to collect lead data from a public directory website. The extracted data was cleaned using pandas by removing duplicates and handling missing values. The final dataset was exported to Excel automatically. 
